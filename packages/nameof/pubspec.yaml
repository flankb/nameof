name: nameof
description: Generator for reflection of class member's names 
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.1 <3.0.0'

dependencies:
  build: ^2.0.0 
  source_gen: ^1.2.0
  build_config: ^1.0.0
  nameof_annotation: 
    path: ../nameof_annotation/

dev_dependencies:
  build_runner: ^2.1.11
  build_test: ^2.1.5
  source_gen_test: ^1.0.2
  lints: ^1.0.0
  test: ^1.16.0
